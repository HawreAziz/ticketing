import { CustomError } from './custom-error';
export declare class EnvironmentError extends CustomError {
    statusCode: number;
    envVariable: string;
    constructor(envVariable: string);
    serializeError(): {
        message: string;
    }[];
}
